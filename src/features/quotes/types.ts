export type QuoteFormState = {
  fromAddress: string;
  toAddress: string;
  moveDateISO: string;
  moveTime: string;
  distanceKm: number;
  itemsCount: number;
  hasPacking: boolean;
  hasStorage: boolean;
};
